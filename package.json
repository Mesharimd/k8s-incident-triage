{
  "name": "k8s-incident-triage",
  "version": "0.1.0",
  "description": "AI incident-triage agent for Kubernetes — read-only tools, cited evidence, human in command",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
